package io.jgym.warmups.day29;

public class SillyShutdownHook {
    public static void main(String... args) {
        // Runtime.getRuntime().halt(0);
        // Runtime.getRuntime().addShutdownHook(new Thread(() -> { while(true); }));
    }
}
